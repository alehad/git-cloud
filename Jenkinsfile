pipeline {
    agent any
    stages {
        stage("build messenger project @ repo level git-cloud") {
            steps {
                echo 'need to configure app build...'
            }
        }
    }
}